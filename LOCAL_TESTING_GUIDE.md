# 🧪 ElderCare Monitor - 로컬 테스트 가이드

## ✅ 테스트 완료!

ElderCare Monitor 시스템이 로컬에서 완벽하게 작동합니다! 

브라우저에서 이미 열려있는 페이지: `file:///c:/Users/yoost/.gemini/antigravity/scratch/elderly-care-monitor/index.html`

---

## 🎮 테스트 방법

### 방법 1: 브라우저 콘솔 사용 (추천)

#### 1단계: 개발자 도구 열기
- **F12** 키를 누르거나
- 마우스 우클릭 → "검사" 선택
- 상단 탭에서 **Console** 클릭

#### 2단계: 테스트 명령어 실행

다음 명령어를 콘솔에 입력하고 **Enter**를 누르세요:

```javascript
// 낙상 시뮬레이션
simulateFall()

// 무호흡 시뮬레이션
simulateApnea()

// 정상 상태로 복귀
simulateNormal()
```

---

## 📋 테스트 시나리오

### 시나리오 1: 정상 상태 확인 ✅

**현재 상태:**
- 상태 배지: "정상" (녹색)
- 심박수: 70-75 BPM
- 호흡수: 14-18 회/분
- 신뢰도: 90-95%

**확인 사항:**
- [ ] 상태 아이콘이 녹색으로 펄스 애니메이션
- [ ] 생체 신호가 정상 범위 내
- [ ] 알림 배너가 표시되지 않음

---

### 시나리오 2: 낙상 감지 🚨

**실행 명령:**
```javascript
simulateFall()
```

**예상 동작:**
1. **즉시 (0초)**: 상태가 "낙상"으로 변경
2. **1초 후**: 빨간색 알림 배너 표시
   - 제목: "이상 징후 감지"
   - 내용: "낙상이 감지되었습니다. 즉시 확인이 필요합니다."
3. **생체 신호 변화**:
   - 심박수: 급증 (100-120 BPM)
   - 호흡수: 증가 (20-25 회/분)
4. **10초 대기 타이머**: 자동으로 카운트다운
5. **10초 후**: 움직임 없으면 긴급 알림 발송 (시뮬레이션)

**확인 사항:**
- [ ] 상태 배지가 "낙상" (빨간색)으로 변경
- [ ] 알림 배너가 빨간색으로 표시
- [ ] 심박수가 급증
- [ ] 감지 이력에 낙상 이벤트 추가
- [ ] 토스트 알림 표시 ("긴급 알림 발송")

**스크린샷 예시:**
- 낙상 감지 시: 빨간색 알림 배너, 상태 "낙상"
- 생체 신호: 심박수 100+ BPM

---

### 시나리오 3: 무호흡 감지 😴

**실행 명령:**
```javascript
simulateApnea()
```

**예상 동작:**
1. **즉시 (0초)**: 상태가 "무호흡"으로 변경
2. **1초 후**: 주황색 알림 배너 표시
   - 제목: "이상 징후 감지"
   - 내용: "무호흡이 감지되었습니다. 확인이 필요합니다."
3. **생체 신호 변화**:
   - 심박수: 감소 (50-60 BPM)
   - 호흡수: 급감 (5-8 회/분)
4. **무활동 시간**: 5분 이상 표시
5. **10초 대기 타이머**: 자동으로 카운트다운
6. **10초 후**: 긴급 알림 발송 (시뮬레이션)

**확인 사항:**
- [ ] 상태 배지가 "무호흡" (주황색)으로 변경
- [ ] 알림 배너가 주황색으로 표시
- [ ] 호흡수가 급감
- [ ] 무활동 시간이 5분 이상으로 표시
- [ ] 감지 이력에 무호흡 이벤트 추가
- [ ] 토스트 알림 표시 ("긴급 알림 발송")

**스크린샷 예시:**
- 무호흡 감지 시: 주황색 알림 배너, 상태 "무호흡"
- 생체 신호: 호흡수 5-8 회/분

---

### 시나리오 4: 정상 상태 복귀 ✅

**실행 명령:**
```javascript
simulateNormal()
```

**예상 동작:**
1. **즉시 (0초)**: 상태가 "정상"으로 변경
2. **알림 배너**: 자동으로 닫힘
3. **생체 신호 정상화**:
   - 심박수: 70-75 BPM
   - 호흡수: 14-18 회/분
4. **무활동 시간**: 0분으로 리셋

**확인 사항:**
- [ ] 상태 배지가 "정상" (녹색)으로 변경
- [ ] 알림 배너가 사라짐
- [ ] 생체 신호가 정상 범위로 복귀
- [ ] 감지 이력에 정상 이벤트 추가

---

## 🎯 고급 테스트

### 테스트 5: 연속 이벤트

```javascript
// 1. 낙상 발생
simulateFall()

// 5초 대기 후...
setTimeout(() => {
    // 2. 정상 복귀
    simulateNormal()
}, 5000)

// 10초 대기 후...
setTimeout(() => {
    // 3. 무호흡 발생
    simulateApnea()
}, 10000)
```

**확인 사항:**
- [ ] 각 이벤트가 순차적으로 감지됨
- [ ] 감지 이력에 모든 이벤트가 기록됨
- [ ] 알림이 각 이벤트마다 발생

---

### 테스트 6: 필터링 기능

**실행 순서:**
1. 여러 이벤트 발생시키기 (낙상, 무호흡, 정상)
2. 감지 이력 섹션으로 스크롤
3. 필터 버튼 클릭:
   - "전체" - 모든 이벤트 표시
   - "정상" - 정상 이벤트만 표시
   - "낙상" - 낙상 이벤트만 표시
   - "무호흡" - 무호흡 이벤트만 표시

**확인 사항:**
- [ ] 필터가 정상 작동
- [ ] 선택한 필터에 해당하는 이벤트만 표시
- [ ] 활성 필터 버튼이 하이라이트됨

---

### 테스트 7: 설정 변경

**알림 설정 테스트:**
1. 설정 섹션으로 스크롤
2. "낙상 감지 알림" 토글 클릭 (OFF)
3. `simulateFall()` 실행
4. 알림이 발생하지 않는지 확인
5. 토글 다시 클릭 (ON)

**임계값 조정 테스트:**
1. "무호흡 감지 시간" 슬라이더 조정 (5분 → 3분)
2. 값이 "3분"으로 표시되는지 확인
3. "감지 민감도" 슬라이더 조정 (7 → 9)
4. 값이 "9"로 표시되는지 확인

**보호자 연락처 테스트:**
1. 전화번호 입력 필드 클릭
2. 새 번호 입력 (예: 010-9876-5432)
3. 이메일 입력 필드 클릭
4. 새 이메일 입력 (예: test@example.com)
5. "설정 저장" 버튼 클릭
6. 토스트 알림 확인 ("설정이 저장되었습니다")

---

### 테스트 8: 차트 시간 범위 변경

**실행 순서:**
1. 실시간 신호 분석 섹션으로 스크롤
2. 차트 컨트롤 버튼 클릭:
   - "1분" - 최근 1분 데이터 표시
   - "5분" - 최근 5분 데이터 표시
   - "15분" - 최근 15분 데이터 표시

**확인 사항:**
- [ ] 차트가 선택한 시간 범위로 업데이트
- [ ] 활성 버튼이 하이라이트됨
- [ ] 데이터 포인트 수가 변경됨

---

## 🐛 문제 해결

### 문제 1: 명령어가 작동하지 않음

**증상:**
```
Uncaught ReferenceError: simulateFall is not defined
```

**해결책:**
1. 페이지를 새로고침 (F5)
2. script.js가 제대로 로드되었는지 확인
3. 콘솔에서 다시 명령어 실행

---

### 문제 2: 알림이 표시되지 않음

**원인:**
- 알림 설정이 꺼져 있을 수 있음

**해결책:**
1. 설정 섹션으로 이동
2. 모든 알림 토글이 ON인지 확인
3. 페이지 새로고침 후 재시도

---

### 문제 3: 차트가 표시되지 않음

**원인:**
- 브라우저가 Canvas를 지원하지 않을 수 있음

**해결책:**
1. 최신 브라우저 사용 (Chrome, Edge, Firefox)
2. 브라우저 캐시 삭제
3. 페이지 새로고침

---

## 📊 테스트 체크리스트

### 기본 기능
- [ ] 페이지 로드 성공
- [ ] 정상 상태 표시
- [ ] 생체 신호 표시 (심박수, 호흡수)
- [ ] 활동 모니터 작동

### 이벤트 감지
- [ ] 낙상 감지 작동
- [ ] 무호흡 감지 작동
- [ ] 정상 복귀 작동
- [ ] 알림 배너 표시

### 이력 및 차트
- [ ] 감지 이력 기록
- [ ] 필터링 기능 작동
- [ ] 실시간 차트 표시
- [ ] 차트 시간 범위 변경

### 설정
- [ ] 알림 토글 작동
- [ ] 슬라이더 조정 작동
- [ ] 연락처 입력 작동
- [ ] 설정 저장 작동

### UI/UX
- [ ] 애니메이션 부드러움
- [ ] 색상 구분 명확 (녹색/빨강/주황)
- [ ] 반응형 디자인 (창 크기 조절)
- [ ] 토스트 알림 표시

---

## 🎬 자동 테스트 스크립트

모든 기능을 한 번에 테스트하려면 콘솔에 다음 스크립트를 붙여넣으세요:

```javascript
async function runFullTest() {
    console.log("🧪 ElderCare Monitor 전체 테스트 시작...\n");
    
    // 1. 정상 상태 확인
    console.log("✅ 1. 정상 상태 확인");
    simulateNormal();
    await new Promise(r => setTimeout(r, 3000));
    
    // 2. 낙상 감지 테스트
    console.log("🚨 2. 낙상 감지 테스트");
    simulateFall();
    await new Promise(r => setTimeout(r, 5000));
    
    // 3. 정상 복귀
    console.log("✅ 3. 정상 복귀");
    simulateNormal();
    await new Promise(r => setTimeout(r, 3000));
    
    // 4. 무호흡 감지 테스트
    console.log("😴 4. 무호흡 감지 테스트");
    simulateApnea();
    await new Promise(r => setTimeout(r, 5000));
    
    // 5. 정상 복귀
    console.log("✅ 5. 최종 정상 복귀");
    simulateNormal();
    
    console.log("\n🎉 전체 테스트 완료!");
    console.log("📊 감지 이력 섹션을 확인하여 모든 이벤트가 기록되었는지 확인하세요.");
}

// 테스트 실행
runFullTest();
```

**실행 방법:**
1. 위 코드 전체를 복사
2. 브라우저 콘솔에 붙여넣기
3. Enter 키 누르기
4. 자동으로 모든 시나리오 테스트 실행

**예상 소요 시간:** 약 20초

---

## 📸 테스트 결과 스크린샷

테스트 중 캡처된 스크린샷들:

### 1. 정상 상태
- 녹색 상태 배지
- 정상 범위 생체 신호
- 알림 없음

### 2. 낙상 감지
- 빨간색 알림 배너
- 상태 "낙상"
- 심박수 급증

### 3. 무호흡 감지
- 주황색 알림 배너
- 상태 "무호흡"
- 호흡수 급감

### 4. 감지 이력
- 모든 이벤트 기록
- 시간 순서대로 표시
- 필터링 가능

### 5. 설정 패널
- 토글 스위치 작동
- 슬라이더 조정 가능
- 연락처 입력 가능

---

## 🚀 다음 단계

### 로컬 테스트 완료 후:

1. **실제 하드웨어 연동**
   - [배포 가이드](DEPLOYMENT_GUIDE.md) 참조
   - mmWave 센서 구매 및 설치

2. **모바일 앱 개발**
   - React Native 또는 Flutter
   - 푸시 알림 통합

3. **클라우드 연동**
   - AWS IoT Core 또는 Google Cloud IoT
   - 실시간 데이터 동기화

4. **파일럿 테스트**
   - 실제 가정 1-2곳에서 테스트
   - 피드백 수집 및 개선

---

## 💡 추가 기능 아이디어

로컬 테스트를 통해 다음 기능들을 추가로 구현할 수 있습니다:

### 1. 음성 알림
```javascript
function speakAlert(message) {
    const utterance = new SpeechSynthesisUtterance(message);
    utterance.lang = 'ko-KR';
    window.speechSynthesis.speak(utterance);
}

// 사용 예시
speakAlert("낙상이 감지되었습니다!");
```

### 2. 데이터 내보내기
```javascript
function exportData() {
    const data = {
        history: detectionHistory,
        settings: {
            fallAlert: document.getElementById('fallAlert').checked,
            apneaAlert: document.getElementById('apneaAlert').checked,
            // ... 기타 설정
        }
    };
    
    const blob = new Blob([JSON.stringify(data, null, 2)], 
        { type: 'application/json' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = 'eldercare-data.json';
    a.click();
}
```

### 3. 주간 리포트 생성
```javascript
function generateWeeklyReport() {
    const report = {
        period: '2024-12-01 ~ 2024-12-07',
        totalEvents: detectionHistory.length,
        fallCount: detectionHistory.filter(e => e.type === 'fall').length,
        apneaCount: detectionHistory.filter(e => e.type === 'apnea').length,
        avgHeartRate: 72,
        avgBreathRate: 16
    };
    
    console.log('📊 주간 리포트:', report);
    return report;
}
```

---

## 📞 문의 및 지원

테스트 중 문제가 발생하면:

- **GitHub Issues**: 버그 리포트
- **Discord**: 실시간 지원
- **이메일**: support@eldercare-monitor.com

---

## ✅ 테스트 완료 확인

모든 테스트를 완료했다면:

- [ ] 기본 기능 모두 작동 확인
- [ ] 이벤트 감지 정상 작동
- [ ] 설정 변경 가능
- [ ] UI/UX 만족스러움

**축하합니다! 🎉**

ElderCare Monitor가 로컬에서 완벽하게 작동합니다.  
이제 실제 배포를 준비하거나 추가 기능을 개발할 수 있습니다.

---

**Made with ❤️ for Elder Care**

*로컬 테스트를 통해 사랑하는 가족의 안전을 지키는 시스템을 완성했습니다.*
